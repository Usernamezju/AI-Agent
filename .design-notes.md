# 临时设计笔记

## 数据流：LLM 输出 → 工具执行

```
parser 解析出 "calculator"  ──►  registry.get("calculator")  ──►  tool.run(expression="15*3+7")
```

## BaseTool 接口契约

为了让 `registry` 和 `agent` 能不关心具体是哪个工具，所有工具都签同一份合同：

| 成员 | 用途 | 谁会用到 |
|------|------|----------|
| `name` | 工具唯一标识，匹配 LLM 输出的 `Action: xxx` | `registry` 做 name→实例映射 |
| `description` | 一句话说明，告诉 LLM 这个工具能干啥 | `registry.generate_descriptions()` 注入 System Prompt |
| `parameters` | JSON Schema，规定参数名、类型、必填项 | LLM 按这个 schema 填 `Action Input` |
| `run(**kwargs)` | 真正执行，返回字符串结果 | `agent` 主循环调用，返回的字符串作为 `Observation` 喂回 LLM |

## 扩展性

```
                        BaseTool (合同)
                       /      |       \
              Calculator  Wikipedia  LocalFileSystem
              .run(exp)  .run(qry)  .run(op, path, content)
```

后续加新工具（发邮件、查数据库等）不用改 `agent.py` 和 `registry.py` 任何代码——实现 `BaseTool` 接口，注册进去就能用。
